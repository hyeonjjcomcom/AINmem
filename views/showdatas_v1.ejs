<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>데이터 조회: <%= dbName %> / <%= collectionName %></title>
    <!-- Tailwind CSS CDN for styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts (Inter) for a clean look -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Apply a clean font stack */
        body {
            font-family: 'Inter', 'Noto Sans KR', sans-serif;
        }
    </style>
</head>
<body class="bg-slate-50 text-slate-800">

    <div class="container mx-auto p-4 sm:p-6 lg:p-8">

        <!-- Header Section -->
        <header class="mb-8">
            <h1 class="text-3xl font-bold text-slate-900">데이터베이스 로그</h1>
            <p class="text-md text-slate-500 mt-1">
                Database: <span class="font-semibold text-slate-600"><%= dbName %></span> / 
                Collection: <span class="font-semibold text-slate-600"><%= collectionName %></span>
            </p>
        </header>

        <!-- Data Display Section -->
        <main>
            <% if (data && data.length > 0) { %>
                <div class="space-y-4">
                    <!-- Iterate over each data entry (document) -->
                    <% data.forEach(item => { %>
                        <% const doc = item.toObject ? item.toObject() : item; %>
                        <div class="bg-white border border-slate-200 rounded-xl shadow-sm hover:shadow-md transition-shadow duration-300 overflow-hidden">
                            <div class="p-5">
                                <!-- Iterate over each key-value pair in the document -->
                                <% Object.keys(doc).forEach(key => { %>
                                    <div class="grid grid-cols-1 md:grid-cols-12 gap-2 py-3 border-b border-slate-100 last:border-b-0">
                                        <!-- Key -->
                                        <div class="md:col-span-2">
                                            <p class="text-sm font-semibold text-slate-500 break-words"><%= key %></p>
                                        </div>
                                        <!-- Value -->
                                        <div class="md:col-span-10">
                                            <% 
                                                let value = doc[key];
                                                // Check if the value is an object or array to pretty-print it
                                                if (typeof value === 'object' && value !== null) {
                                                    value = JSON.stringify(value, null, 2); %>
                                                    <pre class="text-sm text-slate-700 bg-slate-50 p-3 rounded-md whitespace-pre-wrap break-all"><%= value %></pre>
                                                <% } else { %>
                                                    <p class="text-sm text-slate-700 bg-slate-50 p-3 rounded-md break-all"><%= value %></p>
                                                <% } 
                                            %>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <!-- Message when no data is available -->
                <div class="text-center py-20">
                    <svg class="mx-auto h-12 w-12 text-slate-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                        <path vector-effect="non-scaling-stroke" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 13h6m-3-3v6m-9 1V7a2 2 0 012-2h12a2 2 0 012 2v10a2 2 0 01-2 2H4a2 2 0 01-2-2z" />
                    </svg>
                    <h3 class="mt-2 text-lg font-medium text-slate-800">데이터가 없습니다</h3>
                    <p class="mt-1 text-sm text-slate-500">컬렉션에 표시할 데이터가 존재하지 않습니다.</p>
                </div>
            <% } %>
        </main>

    </div>

</body>
</html>
